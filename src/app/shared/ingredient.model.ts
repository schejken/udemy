export class Ingredient {
    // public name: string;
    // public amount: number;

    // constructor(name: string, amount: number) {
    //     this.name = name;
    //     this.amount = amount;
    // }
    // Shortened way to do the abouve section
    constructor(
        public name: string,
        public amount: number) {}
}
